# 抽籤卜卦功能說明

## 已完成的部分

✅ 創建了 `divination.js` - 抽籤互動模組
✅ 創建了 `divination.css` - 抽籤視覺樣式
✅ 已整合到 HTML 中

## 功能特色

### 視覺效果
- 🎋 **籤筒**：古銅色籤筒，內含20支浮動的籤
- 🌊 **搖籤動畫**：點擊按鈕時籤筒會搖動
- ✨ **籤飛出**：抽到的籤會以旋轉動畫飛出
- 📊 **三個籤位**：顯示三次抽籤的結果

### 互動流程
1. 使用者輸入問題
2. 顯示籤筒介面
3. 點擊「搖籤」按鈕（需點3次）
4. 每次搖出一個數字（100-999）
5. 三次完成後自動送出占卜

## 如何啟用

需要修改 `script.js` 中的 `sendMessage` 函數，在發送占卜請求前先顯示抽籤介面。

### 修改步驟

在 `script.js` 的 `sendMessage` 函數中，找到這段：

```javascript
async function sendMessage() {
    const message = userInput.value.trim();
    if (!message) return;
    
    // 顯示用戶訊息
    addMessage(message, 'user');
    userInput.value = '';
    
    // 顯示載入中
    const loadingDiv = addMessage('思考中...', 'bot');
    
    // ... 發送 API 請求
}
```

改成：

```javascript
async function sendMessage() {
    const message = userInput.value.trim();
    if (!message) return;
    
    // 顯示用戶訊息
    addMessage(message, 'user');
    userInput.value = '';
    
    // 顯示抽籤介面
    const drawHTML = divinationDrawer.showDrawInterface(message);
    const drawContainer = document.createElement('div');
    drawContainer.innerHTML = drawHTML;
    chatContainer.appendChild(drawContainer.firstChild);
    chatContainer.classList.add('has-messages');
    
    // 等待使用者完成抽籤...
    // (抽籤完成後會自動觸發 submitDivination)
}
```

## 完整實作（需要進一步整合）

由於需要修改較多邏輯，建議：

1. **簡化版**：保留目前自動生成數字的方式
2. **完整版**：需要重構 API 調用邏輯，讓前端傳遞抽到的數字

您想要我繼續完成完整整合嗎？還是先測試看看抽籤介面的視覺效果？
